<?xml version="1.0" encoding="utf-8"?>

<pets>
    <!-- {{#.}} -->
    <pet species="{{species}}" sex="{{sex}}">
        <name>{{name}}</name>
        <birth>{{birth}}</birth>
        <death>{{death}}</death>
    </pet>
    <!-- {{/.}} -->
</pets>
